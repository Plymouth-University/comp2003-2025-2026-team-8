Requirements:
	- Python 3 installed
	- Node.js installed

Install Python packages:
	- pip install pandas yfinance feedparser vaderSentiment scikit-learn


There are TWO ways of running the web application


Method 1 - Quick Way

Step 1
double-click:
run_app.bat

This should automatically bring up the web app.
To shut down server just go to the original .bat terminal and press any key.


Method 2 - If there are issues with dependencies

Step 1
Open the project folder in IDE

Step 2
Run the backend script inside src folder:
python ML.py (Sometimes may need to run twice if issues occur)

Step 3
Run the database:
cd .\server\
npm install
node server.js

Step 4
Open a new terminal
Start the frontend:
npm install
npm run dev
or
npm run dev -- --host (To run a web server)

Step 5
Open the provided link
